@page
@model UserProfileModel

@{
    ViewData["Title"] = "User Profile";
}

<h1>User Profile</h1>
<form method="post">    
    <div class="form-group">
        <label asp-for="UserProfile.Username">Username</label>
        <input asp-for="UserProfile.Username" class="form-control" disabled />
    </div>
    <div class="form-group">
        <label asp-for="UserProfile.Email">Email</label>
        <input asp-for="UserProfile.Email" class="form-control" disabled="!EditMode" />
    </div>
    <div class="form-group">
        <label asp-for="UserProfile.Role">Role</label>
        <input asp-for="UserProfile.Role" class="form-control" disabled="!EditMode" />
    </div>
    <div class="form-group">


        @if (!Model.EditMode)
        {
            <a asp-page-handler="EditProfile" class="btn btn-primary">Edit Profile</a>
            <button type="submit" class="btn btn-danger" asp-page-handler="DeleteProfile">Delete Profile</button>
        }
        else
        {
            <button type="submit" class="btn btn-success">Save Profile</button>
            <button type="submit" class="btn btn-secondary" asp-page-handler="CancelEdit">Cancel</button>
        }
    </div>
</form>